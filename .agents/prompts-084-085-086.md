# Prompts para Codex (084), Copilot (085) y Antigravity (086)

Cada agente trabaja **en su propia rama**. **Antes de empezar: pull de main** (`git checkout main && git pull origin main`). Luego crear la rama indicada.

---

## Codex — Tarea 084 (rate limiter por provider)

```
Tarea 084: recuperar el rate limiter por provider en main. Sigue EXACTAMENTE .agents/tasks/2026-03-05-084-r16-codex-recuperar-rate-limiter.md.

Antes de empezar: git checkout main && git pull origin main. Luego: git checkout -b codex/084-recuperar-rate-limiter.

IMPORTANTE: Trabaja SOLO en la rama codex/084-recuperar-rate-limiter.

Haz solo:
1. Localizar la rama que tiene worker/rate_limit.py y tests de dispatcher del rate limiter (usa docs/analisis-contenido-perdido-r16.md y los inventarios).
2. Traer solo esos cambios a tu rama (cherry-pick o copiar archivos). No incluir otros features de esa rama.
3. Ejecutar pytest y corregir lo mínimo para que pasen los tests.
4. Abrir PR desde codex/084-recuperar-rate-limiter a main. En la descripción indica de qué rama recuperaste.

No toques Granola, Figma ni otras partes del worker/dispatcher. Solo rate limiter y tests asociados.
```

---

## GitHub Copilot — Tarea 085 (scripts Bitácora)

```
Tarea 085: recuperar los scripts de enriquecimiento de la Bitácora (métricas/resúmenes Notion). Sigue EXACTAMENTE .agents/tasks/2026-03-05-085-r16-copilot-recuperar-bitacora-scripts.md.

Antes de empezar: git checkout main && git pull origin main. Luego: git checkout -b copilot/085-recuperar-bitacora-scripts.

IMPORTANTE: Trabaja SOLO en la rama copilot/085-recuperar-bitacora-scripts.

Haz solo:
1. Localizar la rama que tiene los scripts de enriquecimiento Bitácora (docs/analisis-contenido-perdido-r16.md, branches-cerrados-inventario.md).
2. Traer solo esos scripts y archivos de soporte a tu rama (cherry-pick o copiar). No traer código de worker/dispatcher ni otros features.
3. Comprobar dependencias/imports; documentar en el PR env vars o requisitos si aplica.
4. Abrir PR desde copilot/085-recuperar-bitacora-scripts a main. En la descripción indica de qué rama recuperaste y qué hace cada script.

No modifiques worker, dispatcher ni CI. Solo scripts y docs de Bitácora/Notion.
```

---

## Antigravity — Tarea 086 (browser automation + skill)

```
Tarea 086: recuperar el plan de browser automation en VM y el skill de OpenClaw. Sigue EXACTAMENTE .agents/tasks/2026-03-05-086-r16-antigravity-recuperar-browser-automation.md.

Antes de empezar: git checkout main && git pull origin main. Luego: git checkout -b antigravity/086-recuperar-browser-automation.

IMPORTANTE: Trabaja SOLO en la rama antigravity/086-recuperar-browser-automation.

Haz solo:
1. Localizar la rama con el plan de browser automation y el skill (p. ej. feat/browser-automation-vm-research; usa docs/analisis-contenido-perdido-r16.md).
2. Traer a tu rama solo docs (plan) y skill OpenClaw (SKILL.md o carpeta del skill). Prioridad: docs + skill; no traer código de aplicación salvo que esté estable y documentado.
3. Opcional: crear docs/ramas-recomendadas-borrar-r16.md con la lista de ramas que el análisis #87 marcó para borrar (una línea por rama + razón).
4. Abrir PR desde antigravity/086-recuperar-browser-automation a main. En la descripción indica de qué rama recuperaste.

No toques worker, dispatcher ni CI. Solo documentación y skill.
```
