# Prompts para Codex (087), Copilot (088) y Antigravity (089) — Cierre R16

Cada agente en **su propia rama**. **Antes de empezar: pull de main** (`git checkout main && git pull origin main`).

---

## Codex — Tarea 087 (merge y verificación final)

```
Tarea 087: merge ordenado de PRs #85–#90 a main y verificación final. Sigue EXACTAMENTE .agents/tasks/2026-03-05-087-r16-codex-merge-verificacion-final.md.

Antes de empezar: git checkout main && git pull origin main.

IMPORTANTE: Si tienes permisos para mergear en el repo, mergea los PRs en este orden: #85 → #86 → #87 → #88 → #89 → #90 (omitir los ya mergeados). Resuelve conflictos si aparecen. Luego ejecuta en main: pip install -e ".[test]" y pytest tests/ -q hasta 0 failed. Actualiza .agents/board.md: marca 084–086 como completados (PR #90, #89, #88) y añade una línea "R16 cerrado — PRs mergeados" con el listado y número de tests. Commit y push del board.

Si no tienes permisos para mergear: actualiza igual el board con el orden de merge recomendado y el estado de cada PR, y documenta en un comentario o en el board que el maintainer debe mergear en ese orden y verificar pytest.

Rama de trabajo si haces cambios locales: codex/087-merge-verificacion-final (solo para el commit del board si aplica).
```

---

## GitHub Copilot — Tarea 088 (cierre Bitácora)

```
Tarea 088: documentar scripts de Bitácora y dependencias faltantes. Sigue EXACTAMENTE .agents/tasks/2026-03-05-088-r16-copilot-cierre-bitacora.md.

Antes de empezar: git checkout main && git pull origin main. Luego: git checkout -b copilot/088-cierre-bitacora.

IMPORTANTE: Trabaja SOLO en la rama copilot/088-cierre-bitacora.

Haz solo:
1. Crear docs/bitacora-scripts.md con: qué hace enrich_bitacora_pages.py y add_resumen_amigable.py, cómo ejecutarlos, env vars (NOTION_API_KEY, NOTION_BITACORA_DB_ID), y la lista de las 6 funciones faltantes en notion_client.py y las 3 en notion.py (nombre y descripción/firma sugerida) para un PR futuro.
2. Añadir en README (o CONTRIBUTING) un enlace a docs/bitacora-scripts.md.
3. Abrir PR desde copilot/088-cierre-bitacora a main. Solo documentación; no implementar las funciones ni tocar worker/dispatcher.
```

---

## Antigravity — Tarea 089 (resumen y guía limpieza)

```
Tarea 089: resumen ejecutivo de cierre R16 y guía para borrar ramas. Sigue EXACTAMENTE .agents/tasks/2026-03-05-089-r16-antigravity-resumen-limpieza.md.

Antes de empezar: git checkout main && git pull origin main. Luego: git checkout -b antigravity/089-resumen-limpieza.

IMPORTANTE: Trabaja SOLO en la rama antigravity/089-resumen-limpieza.

Haz solo:
1. Crear docs/r16-cierre-resumen.md con: por cada PR #85–#90 qué se recuperó (una línea por PR), estado final (tests que pasan, CI), y próximos pasos (enlace a bitacora-scripts, ramas a borrar).
2. En docs/ramas-recomendadas-borrar-r16.md (o en docs/guia-borrar-ramas-r16.md) añadir sección "Cómo borrar estas ramas" con instrucciones (git push origin --delete nombre-rama por cada una, o listado de comandos). NO ejecutar borrados; solo documentar.
3. Actualizar .agents/board.md con 089 y referencia a docs/r16-cierre-resumen.md.
4. Abrir PR desde antigravity/089-resumen-limpieza a main. Solo docs; no borrar ramas remotas.
```
