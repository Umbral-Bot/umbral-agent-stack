# Prompts R17 — Claude (090), Codex (091), Copilot (092), Antigravity (093)

Cada agente en **su rama**. **Antes de empezar: pull de main** (`git checkout main && git pull origin main`).

---

## Claude — Tarea 090 (implementar 9 funciones Notion)

```
Tarea 090: implementar las 9 funciones de Notion para que los scripts de Bitácora funcionen. Sigue EXACTAMENTE .agents/tasks/2026-03-06-090-r17-claude-implementar-notion-bitacora.md.

Antes de empezar: git checkout main && git pull origin main. Luego: git checkout -b claude/090-implementar-notion-bitacora.

IMPORTANTE: Trabaja SOLO en la rama claude/090-implementar-notion-bitacora.

Haz solo:
1. Leer docs/bitacora-scripts.md (o los scripts enrich_bitacora_pages.py, add_resumen_amigable.py y tests/test_notion_enrich_bitacora.py) para obtener las 9 funciones con firma.
2. Implementar 6 funciones en worker/notion_client.py y 3 donde corresponda (p. ej. worker/tasks/notion.py).
3. Ejecutar pytest tests/test_notion_enrich_bitacora.py y pytest tests/ -q hasta 0 failed.
4. Abrir PR a main. Título: feat(R17-090): implementar 9 funciones Notion para scripts Bitácora.

No toques dispatcher, CI ni otros handlers. Solo Notion para Bitácora.
```

---

## Codex — Tarea 091 (script dry-run borrado ramas)

```
Tarea 091: script que genere los comandos git push origin --delete para las ramas listadas en la guía. Sigue EXACTAMENTE .agents/tasks/2026-03-06-091-r17-codex-script-borrado-ramas.md.

Antes de empezar: git checkout main && git pull origin main. Luego: git checkout -b codex/091-script-borrado-ramas.

IMPORTANTE: Trabaja SOLO en la rama codex/091-script-borrado-ramas.

Haz solo:
1. Crear scripts/borrar_ramas_r16_dry_run.py (o .sh/.ps1) que lea docs/guia-borrar-ramas-r16.md o docs/ramas-recomendadas-borrar-r16.md y escriba los comandos "git push origin --delete <rama>" (solo imprimir o guardar en .txt). No ejecutar borrados.
2. Añadir en el doc o en el script cómo ejecutarlo.
3. Abrir PR a main. Solo script + doc.

No ejecutar git push --delete. Solo generación de comandos (dry-run).
```

---

## GitHub Copilot — Tarea 092 (changelog R16 en README)

```
Tarea 092: añadir sección Changelog reciente / Estado R16 en README. Sigue EXACTAMENTE .agents/tasks/2026-03-06-092-r17-copilot-changelog-r16.md.

Antes de empezar: git checkout main && git pull origin main. Luego: git checkout -b copilot/092-changelog-r16.

IMPORTANTE: Trabaja SOLO en la rama copilot/092-changelog-r16.

Haz solo:
1. Añadir en README una subsección "Changelog reciente" o "Estado R16" con 2–4 líneas: qué se recuperó (rate limiter, Bitácora, browser automation, inventarios) y enlaces a docs/bitacora-scripts.md, docs/64-browser-automation-vm-plan.md, docs/r16-cierre-resumen.md, docs/guia-borrar-ramas-r16.md. Opcional: tests 866, board.
2. Abrir PR a main. Solo README.

No tocar código de aplicación.
```

---

## Antigravity — Tarea 093 (runbook Bitácora + browser)

```
Tarea 093: actualizar runbook operacional con Bitácora y browser automation. Sigue EXACTAMENTE .agents/tasks/2026-03-06-093-r17-antigravity-runbook-bitacora-browser.md.

Antes de empezar: git checkout main && git pull origin main. Luego: git checkout -b antigravity/093-runbook-bitacora-browser.

IMPORTANTE: Trabaja SOLO en la rama antigravity/093-runbook-bitacora-browser.

Haz solo:
1. En docs/62-operational-runbook.md añadir sección "Scripts y docs recuperados (R16)": Bitácora (enrich_bitacora_pages, add_resumen_amigable, env vars, docs/bitacora-scripts.md), browser automation (docs/64-browser-automation-vm-plan.md, skill browser-automation-vm), guía ramas (docs/guia-borrar-ramas-r16.md).
2. Abrir PR a main. Solo docs.

No tocar worker, dispatcher ni CI.
```
