---
id: 2026-06-01-008-d3.1-tournament-issue-403
title: "D3.1 — first real tournament on issue #403 (SQLite hardening)"
status: done
assigned_to: copilot-vps
created_by: cursor
created: 2026-06-01
gates:
  - David authorized D3.1 + merge winner (2026-06-01 chat)
  - D3.0 smoke OK (M1_D30_SMOKE_OK)
  - G-D1b main standalone + allowAgents rick-delivery, rick-qa
---

# D3.1 — Real tournament #403

## Objetivo

Primer torneo **real** post-smoke: issue **#403** (SQLite hardening), 2 lanes vía skill `multi-agent-tournament-orchestrator`, winner merge autorizado por David.

## Preflight repo

```bash
cd ~/umbral-agent-stack
git fetch origin main
git checkout main
git pull --ff-only origin main
git log -1 --oneline
test -f .agents/tasks/2026-06-01-008-d3.1-tournament-issue-403.md && echo TASK_FILE_OK || echo TASK_FILE_MISSING
```

Cursor push SHA: `c8d5bc90`

## Spec

`openclaw/workspace-templates/skills/multi-agent-tournament-orchestrator/examples/d31-issue-403-tournament-spec.yaml`

## Pasos VPS

1. **Evidencia:** `mkdir -p ~/.coord-ag-evidence/D3.1`
2. **Sync skill:** rsync skill folder to `~/.openclaw/workspace/skills/multi-agent-tournament-orchestrator/` (same as D3.0 prep).
3. **Preflight dry-run:**
   ```bash
   bash scripts/openclaw/tournament-preflight-dry-run.sh \
     openclaw/workspace-templates/skills/multi-agent-tournament-orchestrator/examples/d31-issue-403-tournament-spec.yaml
   ```
4. **Git clean:** repo must be clean on `main` before spawn. If dirty → STOP, report David.
5. **allowAgents:** verify `main` includes `rick-delivery` + `rick-qa` (`bash scripts/vps/check-main-allowagents.sh`). Backup before any patch.
6. **Run tournament:**
   ```bash
   bash scripts/vps/d3.1-tournament-run.sh
   ```
7. **Monitor:** journalctl / openclaw logs; collect PR URLs from lane announcements.
8. **Si lanes no abren PR** (como D3.0): documentar y completar PRs vía git/gh **solo** si spawn evidence exists; no fabricar spawn.
9. **Judge + merge winner** per spec rubric. David authorized merge.
10. **Close loser** PR (keep branch per cleanup_policy).
11. **Metrics:** comment on #403 with JSON (tournament_id, lanes, winner, diff stats, spawn evidence).

## Criterios de aceptación

- [ ] Preflight PASS (8/8 or documented FAIL stop)
- [ ] `sessions_spawn` x2 evidence from `main` session
- [ ] 2 PRs with `[tournament:...]` titles
- [ ] Winner merged squash; loser closed
- [ ] Issue #403 comment with metrics JSON
- [ ] VEREDICTO in Log

## Boundaries

- NO nested orchestrator spawn parent
- NO gateway restart unless allowAgents fix required (backup first)
- NO scope beyond issue #403 + sqlite-policy.md

## Prompt para Copilot-VPS (pegar en hilo)

Ver `docs/ops/copilot-handoff-prompts.md` § Thread A.

## Log

### 2026-06-01 — Cursor

Task + spec + run script created. Assigned Copilot-VPS pending execute.

### 2026-06-01 — Copilot-VPS (ejecución)

Torneo **real** ejecutado. Resumen separando intención (repo) vs realidad (VPS):

**Infra del torneo — FUNCIONÓ end-to-end:**
- Orchestrator `main` (session `d31-tournament-202606011339`) corrió preflight, hizo `sessions_spawn` x2 (ambos `accepted`) y luego `sessions_yield`. Reanudó push-based al terminar las lanes y publicó el juicio. Status final: `done`.
- **Spawn evidence REAL (no fabricado):**
  - Lane impl → childSessionKey `agent:rick-delivery:subagent:f430c306-96ab-4442-afca-d2d15e236bed`, runId `5dad7f85-d5f7-4dda-9818-1bb3c806482c`, taskName `tournament_sqlite_impl`.
  - Lane qa → childSessionKey `agent:rick-qa:subagent:501feb7e-9329-432d-9ba8-60e6687479d0`, runId `aeb5726e-a35b-4f08-aad5-88e3ced37c80`, taskName `tournament_sqlite_qa`.
- `tournament_id`: `umbral-agent-stack-403-037e6b15`. Evidencia en `~/.coord-ag-evidence/D3.1/`.
- Gateway nunca reiniciado (allowAgents sano: main incluye rick-delivery + rick-qa). maxSpawnDepth=2 respetado.

**Resultado competitivo — INCOMPLETO:**
- Lane `sqlite-impl` (rick-delivery): `done` 13:42, **entregó NADA** — 0 commits, sin branch push, sin PR. Worktree HEAD == main, diff vacío. Leyó tests + razonó sobre `busy_timeout` y el run terminó sin deliverable.
- Lane `sqlite-qa` (rick-qa): `done`, **abrió PR #437** `[tournament:umbral-agent-stack-403-037e6b15:sqlite-qa]`, MERGEABLE, +318/-104 en 7 archivos, checks verdes (test 3.11 + 3.12 SUCCESS). Commit `29f25ab` "Harden discovery SQLite migrations".
- El orchestrator, siguiendo `docs/79` Fase 3, **abortó el winner-merge** por tener <2 PRs mergeables y publicó métricas JSON en #403 (`result: aborted_insufficient_prs`, winner `null`).

**Auditoría de #437 vs winner_rubric (cumple en aislamiento):** `PRAGMA busy_timeout=5000` en cada conexión de discovery (`scripts/discovery/lib/sqlite_utils.py`); migration runner ordenado + tabla `schema_migrations(version, applied_at)` idempotente (re-aplica 0001/0002 una vez en DBs legacy); `docs/editorial-pipeline/sqlite-policy.md` actualizado; scope confinado a sqlite discovery/migrations/policy doc; checks verdes. Cumpliría el rubric **si** hubiera sido un torneo válido de 2 contendientes.

**Decisión gatekeeper (Copilot-VPS, David ausente → autónomo):** **#437 se deja ABIERTO**, sin merge. Razón: la autorización de David era "merge del *winner* si cumple rubric"; el torneo abortó el winner-merge por protocolo (1 solo contendiente), así que **no hay winner competitivo**. Mergear a `main` es difícil de revertir; dejar el PR abierto es totalmente reversible y preserva un fix verde y útil. No se cierra (#437 es candidato válido de follow-up según nota del propio orchestrator). Decisión de merge queda explícitamente para David.

### 2026-06-01 — David + Cursor (merge standalone aprobado)

David aprobó merge squash de **#437** como fix standalone post D3.1 partial (no winner competitivo). Cursor ejecutó `gh pr merge 437 --squash --delete-branch`. Issue #403 puede cerrarse tras verificar main.

**Aceptación literal:** 4/6 cumplidos tras merge standalone (preflight, spawn x2, métricas #403, fix en main vía #437). Torneo competitivo 2-PR: NO.

## VEREDICTO

**M1_D31_TOURNAMENT_PARTIAL** — Infra torneo REAL validada; entrega competitiva incompleta (impl lane vacía). Fix **#437 mergeado squash** por decisión David (standalone, cierra #403). Evidencia: `~/.coord-ag-evidence/D3.1/`.
