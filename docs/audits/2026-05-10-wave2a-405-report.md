# Wave 2.A — #405 stop button + runtime flags — validation report

**Date**: 2026-05-10
**Issue**: #405
**Branch**: `copilot-vps/wave2a-405-stop-button`
**Status**: ready for review

## What landed

- `docs/editorial-pipeline/runtime-flags-contract.md` (contract first).
- `scripts/discovery/lib/publish_flags.py` (implementation).
- `tests/lib/test_publish_flags.py` (unit).
- `tests/discovery/test_publish_guard_flags_integration.py` (integration).
- `docs/runbooks/publish-emergency-stop.md` (rollback path).
- Minimal integration in `scripts/discovery/lib/publish_guard.py`:
  - New optional parameter `flags: PublishFlags | None = None`.
  - When `flags` is supplied AND `flags.allows_real_publish() is False`,
    a `publish_guard.dry_run` marker is emitted to ops_log before the
    existing `publish_guard.pass` line.
  - When `flags` is omitted, behaviour is byte-identical to pre-#405
    (verified by `test_call_without_flags_preserves_legacy_behavior`).

## Repo dice X / VPS muestra Y

| Repo claim | VPS verification |
|---|---|
| Defaults are fail-closed | Smoke run with `from_env({})` → `allows_real_publish=False` |
| Garbage env never raises | Smoke with `maybe / sometimes / -3 / abc` returned defaults and `allows_real_publish=False`; warnings logged, no exception |
| All-on env enables real publish | Smoke with `true / false / 1 / 1` → `allows_real_publish=True` |
| Suite green | pytest tail: `470 passed in 26.32s` |
| Stage 7.5 untouched | `git diff main -- scripts/discovery/stage7_5_*` → 0 lines |
| `variants.py` untouched | `git diff main -- scripts/discovery/lib/variants.py` → 0 lines |
| No real writes added | New code only emits ops_log lines; no HTTP client / no publisher invocation |

### Smoke literal output

```
default env: PublishFlags(publish_enabled=False, dry_run=True, max_posts=1, max_posts_per_day=1)
  allows_real_publish: False
all-on env: PublishFlags(publish_enabled=True, dry_run=False, max_posts=1, max_posts_per_day=1)
  allows_real_publish: True
garbage env: PublishFlags(publish_enabled=False, dry_run=True, max_posts=1, max_posts_per_day=1)
  allows_real_publish: False
```

Warnings emitted by `publish_flags` for the garbage scenario:

```
publish_flags: unparseable bool for PUBLISH_ENABLED='maybe' — falling back to default=False
publish_flags: unparseable bool for DRY_RUN='sometimes' — falling back to default=True
publish_flags: MAX_POSTS=-3 below min_value=0 — falling back to default=1
publish_flags: unparseable int for MAX_POSTS_PER_DAY='abc' — falling back to default=1
```

## DoD checklist

- [x] 1. Contract before code (`runtime-flags-contract.md` committed
  alone in `b4b22f9` ahead of any implementation commit).
- [x] 2. Unit tests (`tests/lib/test_publish_flags.py`).
- [x] 3. Integration test
  (`tests/discovery/test_publish_guard_flags_integration.py`).
- [x] 4. VPS smoke (suite + flags smoke ran on the VPS in
  `/home/rick/umbral-agent-stack`; logs in `/tmp/wave2a-405-*.log`).
- [x] 5. Repo vs VPS report (this file).
- [x] 6. No real writes (no HTTP client, no publisher invocation, no
  external API call introduced).
- [x] 7. Rollback path documented
  (`docs/runbooks/publish-emergency-stop.md`).
- [x] 8. What this does NOT solve (below + in the contract).

## What this does NOT solve

- Daily-cap enforcement against real `published_history` rows. The
  `MAX_POSTS_PER_DAY` flag is parsed and exposed; enforcement waits for
  #402 (`publication_content_hash`) + #404-lite (publish log).
- Real publish target wiring (blog `umbralbim.cl`, LinkedIn).
- Cron / timer scheduling.
- Token rotation (referenced from emergency-stop step 3 → #406).
- Operator-facing dashboard.

## Carry-overs to next issues

- **#402**: consume `PublishFlags` directly in the E2E dry-run test
  instead of stubbing env. The integration test in this PR is the
  template.
- **#404-lite**: include `flags.publish_enabled`, `flags.dry_run`,
  `flags.max_posts`, `flags.max_posts_per_day` in every JSONL event for
  traceability. The `publish_guard.dry_run` marker is the first such
  event and already carries those fields.
- **#406**: `docs/runbooks/secrets-tokens.md` is referenced from
  emergency-stop Step 3.
