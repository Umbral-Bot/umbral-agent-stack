# Audit 2026-05-14 — O16.2 task 047 gap-closure

**Tarea**: 2026-05-07-047 — pdf-parser (Document Intelligence prebuilt-layout) + Container Apps Job
**Branch**: `copilot/feat-o16-2-047-gap-closure`
**Workspace**: `C:\GitHub\.tmp-o16-2-047` (clone limpio desde `origin/main` @ `ef053c3`)
**Autorizado por**: David, 2026-05-14, opción A de D-047
**Operador**: Coordinador de Agentes (custom Copilot agent)
**Modo**: gap-closure (no scaffold — todos los entregables ya existían)

---

## Contexto del cambio de alcance

La task 047 fue solicitada inicialmente como "scaffold reversible". El audit previo descubrió que **todos los entregables del spec ya estaban implementados y mergeados a `main`** en commits previos:

| Entregable spec 047 | Estado en `origin/main` |
|---|---|
| `scripts/aeco-kb/pdf_parser.py` (módulo + CLI) | ✅ existe (≈340 LOC) |
| `infra/docker/aeco-pdf-parser/Dockerfile` | ✅ existe |
| `infra/docker/aeco-pdf-parser/entrypoint.sh` | ✅ existe |
| `infra/azure/modules/aeco-pdf-parser-job.bicep` | ✅ existe |
| `pyproject.toml` opt-dep `aeco-kb` | ✅ declarado con `azure-ai-documentintelligence>=1.0.0,<2.0.0` + `azure-storage-blob>=12.19.0,<13.0.0` |
| `scripts/aeco-kb/README.md` | ✅ existe |

**Gaps reales identificados antes de este PR**:

1. **0 tests** para `pdf_parser.py` — ni de chunking, ni de schema, ni de CLI parsing.
2. **`--dry-run` flag no es realmente local** — invoca `download_blob` + `parse_pdf_with_di` ANTES de saltarse solo el upload. Imposible probar sin Azure creds.
3. **Módulo Bicep no está wireado** en ningún umbrella (`main.bicep` ni `aeco-kb-pipeline.bicep` lo referencian). El job sólo se desplegaría con un deployment file dedicado.

Este PR cierra (1). Documenta (2) y (3) como riesgos abiertos.

---

## Cambios aplicados en este PR

| Archivo | Tipo | Descripción |
|---|---|---|
| `tests/test_pdf_parser.py` | NEW | 16 tests unitarios sin Azure SDK: chunking, table-to-md, Chunk schema vs índice 046, parse_args. |
| `docs/audits/2026-05-14-o16-2-047-gap-closure.md` | NEW | Este documento. |

**Cambios NO aplicados** (deliberadamente fuera de alcance):

- ❌ No se modificó `pdf_parser.py` (no se detectaron bugs reales).
- ❌ No se modificó `aeco-pdf-parser-job.bicep` (compila clean, no requiere ajuste).
- ❌ No se wireó el módulo en ningún umbrella (requeriría decisión arquitectónica + autorización para deploy).
- ❌ No se ejecutó `--dry-run` real (requiere Azure creds + blob real).
- ❌ No se desplegó nada en Azure.

---

## Validaciones ejecutadas

### 1. `pytest tests/test_pdf_parser.py`

Comando:
```powershell
& C:\GitHub\umbral-agent-stack\.venv\Scripts\python.exe -m pytest tests\test_pdf_parser.py -v
```

Resultado: **16 passed in 0.77s**

Tests cubren:

| Área | Test | Verifica |
|---|---|---|
| Heurística tokens | `test_estimate_tokens_empty`, `test_estimate_tokens_scales_with_words` | `words * 1.3`, monotonicidad |
| Chunking | `test_chunk_paragraphs_merges_short_neighbors` | Párrafos < 50 tokens se mergean |
| Chunking | `test_chunk_paragraphs_splits_oversized` | Párrafos > 800 tokens se dividen y cada chunk es < 50% del original |
| Chunking | `test_chunk_paragraphs_prepends_heading` | Headings se prepend como `## ...` markdown |
| Chunking | `test_chunk_paragraphs_skips_empty` | Whitespace/newlines no generan chunks |
| Tablas | `test_table_to_markdown_basic` | Header + separator + body markdown válido |
| Tablas | `test_table_to_markdown_escapes_pipe` | `|` literal escapado a `\|` |
| Schema | `test_chunk_dataclass_carries_all_index_fields` | Chunk contiene los 13 campos del schema 046 |
| Schema | `test_chunk_serializes_to_json` | JSON serialization sin parser_-prefixed fields |
| CLI | `test_parse_args_minimal_ok` | Args mínimos requeridos parsean OK |
| CLI | `test_parse_args_missing_required_exits` | SystemExit si faltan |
| CLI | `test_parse_args_rejects_invalid_source_type` | Choices enforcement |
| CLI | `test_parse_args_env_var_fallback` | env vars override defaults |
| Constantes | `test_parser_version_locked` | `PARSER_VERSION == "v1.0.0"` |
| Constantes | `test_di_model_locked_to_prebuilt_layout` | Decisión D7 spec 047 |

### 2. `az bicep build` — módulo standalone

Comando:
```powershell
az bicep build --file infra\azure\modules\aeco-pdf-parser-job.bicep --outfile $env:TEMP\aeco-pdf-parser-job.json
```

Resultado: **EXIT 0, 4865 bytes ARM JSON, 0 warnings, 0 errors** (warning Bicep upgrade-disponible es informativo).

### 3. `az bicep build` — umbrella `infra/azure/main.bicep`

Comando:
```powershell
az bicep build --file infra\azure\main.bicep --outfile $env:TEMP\main-umbrella.json
```

Resultado: **EXIT 0, 68258 bytes ARM JSON, 0 warnings, 0 errors**.

Búsqueda `pdf.parser|aeco-pdf-parser` en `main.bicep`: **0 matches** → módulo no está wireado en umbrella (esperado / documentado como riesgo).

### 4. `az bicep build` — `infra/azure/aeco-kb-pipeline.bicep`

Comando:
```powershell
az bicep build --file infra\azure\aeco-kb-pipeline.bicep --outfile $env:TEMP\aeco-pipe.json
```

Resultado: **EXIT 0, 24877 bytes ARM JSON, 0 warnings, 0 errors**.

Búsqueda `pdf.parser|aeco-pdf-parser` en `aeco-kb-pipeline.bicep`: **0 matches** → módulo tampoco wireado acá.

### 5. Dry-run local del CLI — **NO ejecutado**

Razón: el flag `--dry-run` actual sólo skipea el upload final. La pipeline `existing_parser_version` → `download_blob` → `parse_pdf_with_di` requiere Azure creds + blob real. No se puede correr 100% local sin refactor.

Cobertura alternativa: tests unitarios sobre `chunk_paragraphs` y `_table_to_markdown` ejercen la lógica pura sin depender del path Azure.

---

## Riesgos abiertos (post-gap-closure)

| # | Severidad | Descripción | Mitigación sugerida |
|---|---|---|---|
| R1 | Media | `--dry-run` no es local — requiere Azure | Refactor menor: agregar `--mock-di` que use paragraphs sintéticos. Out of scope acá. |
| R2 | Media | Módulo Bicep no está wireado en ningún umbrella → no hay deployment path actual | Decidir si va en `aeco-kb-pipeline.bicep` (cohesión) o nuevo `aeco-pdf-parser.bicep` deployment file (aislamiento). Bloquea el smoke real DI. |
| R3 | Baja | Heurística de tokens (`words*1.3`) puede sub-estimar; chunks reales pueden exceder `TOKEN_TARGET_MAX` por ~10-15% | Documentado en test `test_chunk_paragraphs_splits_oversized`. Q3: evaluar `tiktoken` si retrieval quality cae. |
| R4 | Baja | GHCR PAT secret se inyecta en el Bicep como `@secure() param` → debe pasarse via `--parameters` en CLI o leerse de Key Vault | Spec 047 ya menciona Q3 migración a ACR privado; mientras tanto, deployment manual con PAT. |
| R5 | Informativa | No hay smoke con PDF real; no se sabe si `prebuilt-layout` produce output útil para specs IFC/MINVU | Bloqueado hasta R2 + sample PDF subido a `crudos/aeco/raw/buildingsmart/`. |

---

## Recomendación de status para 047

**PARTIAL**

Justificación:

- Código + Bicep + Docker + tests unitarios → **PASS**.
- Wiring en umbrella + smoke real con DI → **PENDIENTE** (R2).
- Refactor `--dry-run` local-friendly → **PENDIENTE** (R1, no bloquea pero útil para CI).

No es PASS porque la decisión "047 funciona end-to-end" requiere R2 + R5. No es FAIL porque toda la base está sólida y testeada.

---

## Recomendación próximo paso

Dos rutas posibles, en orden de preferencia:

### Ruta preferida: cerrar R2 antes de seguir a 048

1. **D-047-bis**: pedir autorización para wirear `aeco-pdf-parser-job.bicep` en `aeco-kb-pipeline.bicep` con un parámetro `deployPdfParser bool = false` (default false → deployment idempotente sin tocar nada nuevo, pero ya disponible para activar con un solo `--parameters`).
2. **D-047-ter**: una vez wireado y autorizado, ejecutar `az deployment group what-if` (read-only de Azure) para validar que el deployment de la pipeline no rompe nada.
3. **D-047-quater**: si what-if OK, desplegar con `deployPdfParser=true` + correr smoke con sample PDF.

### Ruta alternativa: avanzar a 048 (crawler) en paralelo

- 048 es independiente de 047 a nivel de scaffolding (escribe a `crudos/aeco/raw/`, no consume el parser).
- Si querés mantener el scaffold de 047 PARTIAL "estable" y avanzar a 048, no hay bloqueo técnico. R2 quedaría pendiente para cuando se quiera activar el smoke real.

### Sobre 044

- 044 (smoke-deploy de la pipeline base) está marcado "paused" en el board. Su deployment **no incluye** el pdf-parser job (R2). Autorizar 044 desplegaría la pipeline AI Search + Storage + DI + UAMI + Cosmos + ServiceBus + Budget alerts, pero NO el job. Eso es OK como preparación de la infraestructura subyacente que 047 necesita para correr — recomiendo autorizar 044 antes que el smoke real de 047.

---

## Evidencia

| Path | Contenido |
|---|---|
| `C:\GitHub\.tmp-o16-2-047\tests\test_pdf_parser.py` | 16 tests, todos pasando |
| `docs/audits/2026-05-14-o16-2-047-gap-closure.md` | este documento |

Outputs de validaciones (transcript completo): se mantiene en el log de la sesión Copilot Chat 2026-05-14. No se generó archivo separado bajo `.coord-ag-evidence/` porque toda la salida quedó en línea acá.

---

## Acciones NO ejecutadas

- ❌ No se modificó nada en `C:\GitHub\umbral-agent-stack` (worktree principal sucio respetado).
- ❌ No se hizo `az deployment ...`, `az * create/update/delete`, ni `az containerapp job start`.
- ❌ No se tocó OpenClaw, VPS, Notion, n8n, RRSS.
- ❌ No se mergeó ningún PR. PR draft creado para review.

---

## Decisión requerida para cerrar 047

Elegí UNA:

- **A) Aceptar 047 como PARTIAL y avanzar a 044 deploy autorizado** (mi recomendación).
- **B) Aceptar PARTIAL y avanzar a 048 (crawler) sin desbloquear smoke 047 todavía**.
- **C) Pedir D-047-bis ya: wirear módulo en `aeco-kb-pipeline.bicep` con flag opcional**.
- **D) Otra cosa**.
