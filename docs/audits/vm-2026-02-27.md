# Auditoría VM (Execution Plane) — 2026-02-27

> **Fecha:** 2026-02-27
> **Ejecutado por:** cursor-agent-cloud
> **Entorno:** VM Windows (PCRick) vía Remote-SSH PowerShell en `C:\GitHub\umbral-agent-stack`

## Estado capturado

| Componente | Estado | Detalle |
|------------|--------|---------|
| **Host** | OK | pcrick\rick, PCRick, `C:\GitHub\umbral-agent-stack` |
| **Repo** | OK, desactualizado | Tiene `.git`, branch `main`, HEAD = `5deb667` (VM está **varios commits por detrás** de origin/main: faltan AGENTS.md, .agents/, docs 16–17, auditorías) |
| **Python** | OK | 3.11.9 |
| **Servicio NSSM** | OK | openclaw-worker: Running, StartType Automatic |
| **Puerto 8088** | OK | Escucha en 0.0.0.0:8088 (PID 15348) |
| **Worker health** | OK | localhost:8088 responde (sin error en la prueba) |
| **Tailscale** | OK | IP 100.109.16.40 (coincide con WORKER_URL usada en VPS) |

## Git en VM

- **HEAD:** 5deb667 — fix: force-set WORKER_TOKEN in tests + deploy script uses consistent token  
- **Faltan en VM:** commits posteriores (39628a1 AGENTS.md, protocolo .agents/, docs 16, 17, docs/audits/vps-2026-02-27.md, etc.).  
- **Recomendación:** En la VM ejecutar `git pull origin main` para alinear con el repo (y tener .agents/, docs de auditoría, etc.). No es bloqueante para el Worker; solo para tener la misma base de código y documentación.

## Estructura del repo en VM

Carpetas visibles: .git, .pytest_cache, changelog, client, dispatcher, docs, infra, openclaw, runbooks, scripts, tests, worker. Archivos: .editorconfig, .env.example, .gitignore, README.md. No se listó .agents/ (porque en 5deb667 aún no existía).

## Resumen

- **Execution Plane operativo:** Worker corriendo como servicio (NSSM), escuchando en 8088, alcanzable por Tailscale (100.109.16.40). E2E VPS → Redis → VM Worker ya validado.
- **Acción sugerida:** `git pull origin main` en la VM cuando convenga, para tener la última versión del repo.
