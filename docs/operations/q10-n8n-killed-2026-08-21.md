# n8n VPS: export-or-kill + RRSS cerrado — closeout (PKG-MACRO-P5-Q10-T1, 2026-08-21)

**Q10 = A:** export-or-kill de los 4 wf VPS; RRSS-vía-n8n = no. Reconteo E6
fila 25 / E-p11. Este es el closeout canónico: **donde choque, gana éste.**

## Qué estaba mal

E-p11 (2026-08-12) encontró 9 workflows en el n8n VPS: solo B1/B3 con
`active=true` (nombre dice `(INACTIVE)`, es drift conocido, no tocado en este
pack — resuelto al día siguiente, ver
[T8](t8-n8n-b1b3-rename-2026-08-22.md)). Los
otros 7 — 3 "Editorial \*", "Laboral Gate Humano", "SIM Diario - Embudo",
"Reporte Ejecutivo Diario", "Correo de prueba diario 08:00" — `active=false`,
sin versionar en `infra/n8n/workflows/`. Doctrina README: sin export, no
existen.

## Qué se hizo

- **KEEP** (verificados `active=true`, intactos): `7tNkwd7DkVCEI2oz` (B1),
  `iazEnIPcxCbipnKQ` (B3).
- **Killed** (export vía CLI `n8n export:workflow`, scrub de secretos Y,
  copiados a `infra/n8n/archive/killed-2026-08-21/` + `INDEX.txt`, luego
  borrados vía `DELETE /api/v1/workflows/{id}` — la CLI instalada 2.10.2 no
  tiene `delete:workflow`, confirmado con `--help`; el endpoint es el
  documentado en el OpenAPI que sirve la propia instancia):
  `11b62f9c-…` (SIM Diario), `b1ba0c1d-…` (Reporte Ejecutivo), `8zzOGYhzzKTnRdJz`
  (Correo de prueba), `qLnjqF2iJdOnVH5U` (Editorial Shortlist),
  `fGrPzz61czmkSrxj` (Editorial Gate), `UhbnM6bUAG86Trgw` (Editorial
  Multicanal), `ydOLYZcq8aJnX6Sg` (Laboral Gate Humano).
- Post-delete verificado: 2 workflows en el VPS, ambos `active=true` (B1/B3),
  cero `active=false`.
- `docs/60-rrss-pipeline-n8n.md`: banner HISTÓRICO/CERRADO, diseño no
  reescrito. `infra/n8n/README.md`: nota de cierre.

## Qué NO se hizo, a propósito

`openclaw.json` sin mutar. `WORKER_TOKEN` y credenciales Telegram sin tocar.
Nada importado a n8n Cloud. `infra/n8n/workflows/` sigue con solo los 2 JSON
de B1/B3 — el governance test no se ensanchó a `archive/`. Sin restart de
worker/gateway.
