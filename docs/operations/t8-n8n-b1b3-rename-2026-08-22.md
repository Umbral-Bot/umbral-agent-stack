# n8n B1/B3: rename sin sufijo (INACTIVE) — closeout (PKG-MACRO-P5-T8-T1, 2026-08-22)

Residual LOTE-1 post Q10. Si choca con
[docs/operations/q10-n8n-killed-2026-08-21.md](q10-n8n-killed-2026-08-21.md)
o `infra/n8n/README.md` (versión previa), **gana este.**

## Qué estaba mal

`7tNkwd7DkVCEI2oz` (B1) y `iazEnIPcxCbipnKQ` (B3), únicos workflows vivos del
n8n VPS, tenían `active=true` pero el *nombre* seguía terminando en
`(INACTIVE)` — mentía. El JSON de B3 en git además tenía `active=false`,
desfasado del live (`true`).

## Qué se hizo

- Inventario live confirmó exactamente esos 2 ids, ambos `active=true` — sin
  BLOCKED.
- Rename: se sacó el sufijo literal `" (INACTIVE)"` del `name` de ambos vía
  `PUT /api/v1/workflows/{id}` (la CLI n8n 2.10.2 no tiene verbo de
  rename/update de nombre — confirmado con `--help`; el endpoint se verificó
  contra el OpenAPI que sirve la propia instancia). Body mínimo: solo
  `name/nodes/connections/settings/staticData`, nada de campos read-only.
  Nodos, credenciales, cron y webhook sin tocar.
- Verificación inmediata post-rename (la "trampa" del PATCH que apaga
  `active`): ambos siguieron `active=true` sin necesidad de reactivar.
- Sample B3 (solo lectura): últimas 20 ejecuciones, 100% `success`, cron cada
  15 min, última `id=2877` @ `2026-08-23T03:45:23Z` UTC.
- Re-export CLI de ambos, scrub de secretos, reemplazo de
  `infra/n8n/workflows/{telegram-ok-publica-b1,worker-health-cron-b3}.json`
  — `active` ahora coincide con el live (`true` en los dos). El export crudo
  trae `id` reales de credenciales/webhook; se restituyó `REPLACE_ON_IMPORT`
  antes de commitear (convención README §4).

## Qué NO se hizo, a propósito

`openclaw.json`, `WORKER_TOKEN`, credenciales Telegram, bot TEST: intactos.
Sin restart de worker/gateway (no hizo falta). Sin cutover a bot de prod.
Nada importado a n8n Cloud, sin usar el MCP Cloud. El PUT omitió `tags`/
`shared` del body (no son `required` en el schema); ambos workflows no
tenían tags asignados, así que no había nada que perder — sin canary previo,
razonado contra el schema. `worker/n8n_client.py::update_workflow()` ya
existe para este mismo PUT; se usó curl directo por la verificación manual
paso a paso que pedía este pack, no por falta de alternativa — próxima vez,
preferir el cliente.
